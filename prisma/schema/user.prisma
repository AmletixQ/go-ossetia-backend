model User {
  id String @id @default(cuid())

  email    String @unique
  password String

  firstName String @map("first_name")
  lastName  String @map("second_name")
  age       Int?

  favourites Event[]
  review     Review?

  @@map("users")
}
